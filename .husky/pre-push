#!/bin/sh
echo "Running pre-push full validation (Frontend)..."
npm run validate:full || {
  echo ""
  echo "ERROR: Full validation failed. Fix errors before pushing."
  exit 1
}
echo ""
echo "Pre-push full validation passed. Ready to push."
