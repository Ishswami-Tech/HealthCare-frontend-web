#!/bin/sh
echo "Running pre-push validation (Frontend)..."
npm run build || {
  echo ""
  echo "ERROR: Build failed. Fix errors before pushing."
  exit 1
}
echo ""
echo "Pre-push validation passed. Ready to push."
